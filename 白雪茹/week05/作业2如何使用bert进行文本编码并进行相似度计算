作业2：使用 BERT 进行文本编码与相似度计算的技术方案（对齐业务需求）

下面给你一份“能直接给团队开工”的方案说明，按 业务需求→技术路线→训练→上线→评估 的结构写。

1）业务需求拆解（你要在方案里写清楚）

用户输入一句“产品咨询问题”

系统从 FAQ 知识库里找出语义最相近的历史问法

返回对应答案（支持 Top1 自动回复或 Top3 让用户点选）

知识库可运营：类目、FAQ、生效时间、变体管理

能持续学习：从日志中挖掘新问法、去重、提升命中率

性能：低延迟（一般 < 200ms～500ms，取决于规模）、可水平扩展

2）总体技术架构（推荐：双塔召回 + 可选精排）

阶段A：语义召回（必选）

模型：SBERT（BERT 双塔）

输入：一句话

输出：句向量（embedding，维度例如 768）

检索：向量库 TopK（cosine/inner product）

阶段B：精排（可选，但更容易把 Top1 做稳）

模型：Cross-Encoder（BERT 拼接 [CLS] Query [SEP] Candidate [SEP]）

对 TopK 候选逐对打分

输出：更精确的相关性分数

用于：Top1 自动回复的可靠性提升

如果你只要求 Top3=94%，召回可能够；如果要“自动回复少出错”，精排很值得加。

3）BERT 如何做“文本编码”
3.1 为什么不用“原生BERT CLS向量直接算相似度”

原生 BERT 的 [CLS] 表示并不是为“句子语义相似度”专门训练的，直接 cosine 往往不稳定。
SBERT/SimCSE 的价值是：让 BERT 输出的句向量在向量空间里具备可比较性。

3.2 SBERT 的编码方式（建议 mean pooling）

Tokenize → BERT 输出每个 token 的 hidden states

用 attention_mask 做 mean pooling 得到句向量

对句向量做 L2 normalize

相似度：cosine = dot(u, v)（归一化后点积即余弦）

4）无监督 SimCSE 预训练怎么做（对齐你的项目描述）

核心思想：同一句子在训练时经过两次 dropout，得到两个不同的向量表示，把它们当作正样本对；同 batch 里其它句子当作负样本。

输入：一句话 s

两次前向：得到 z1, z2（dropout 不同）

Loss：InfoNCE（让 z1 和 z2 更近；与其他句子更远）

得到一个“适合做语义相似度”的 encoder 初始权重

5）再用业务数据做 SBERT 微调（把效果拉到可用）

你业务里最有价值的是：FAQ 变体、以及日志点击/人工选择。

训练样本：

正例：用户问句 / 变体问法 / 标准问法（属于同一 faq_item）

难负例：同类目不同 faq_item（非常关键）

负例：跨类目随机

目标：

召回 TopK 更稳（Top3 提升）

类目内的“相似但不同答案”的区分能力增强（减少错答）

损失：

多负例对比损失（in-batch negatives）

或 Triplet loss（anchor/positive/negative）

6）上线检索策略（必须写清楚：阈值 + Top3 + 兜底）

在线流程：

query 预处理（全半角、空白、繁简、特殊符号、同义词映射可选）

encoder 输出 query embedding

向量库 TopK 召回（过滤：status=生效 + 生效时间窗）

若有精排：对 TopK 做 Cross-Encoder 打分 → 取 Top3

置信度判断：

Top1 分数 ≥ T1：直接回复答案

否则但 Top3 有一定分数：返回 Top3 候选问题让用户选

都低：转人工/引导留资，并把问题进入“待补充FAQ池”

这一步是“代替人工客服”能否安全落地的关键：不要低置信度时硬答。

7）文本去重与“相似提问”生成

你提的“文本去重；FAQ 匹配”可以这么写：

硬去重：规范化文本 → hash → 完全重复直接合并

近重复：

用 embedding 聚类（同一簇候选重复）

或对候选对做 cosine ≥ T_dup 认为重复

输出给运营后台：

建议合并的问法对

以及建议挂到哪个 faq_item 下面作为 similar_question

8）评估指标（建议你在方案里写出你那句 Top3=94% 如何定义）

离线：

Recall@K / TopK Accuracy（Top1/Top3/Top5）

MRR / nDCG（排序质量）

按类目分桶评估（某些类目更难）

在线：

命中率（可回复比例）

自动回复采纳率/用户点击率

转人工率

误答率（抽检/用户差评）

平均响应时延
