1. BERT 分类与实体识别的关系
在这段代码中，两者是共同特征提取、不同输出头的关系：
共享底层： 两者共用同一个 BERT 预训练模型作为特征提取器（Encoder）。BERT 输出的隐状态包含了句子的语义信息。
分类（Intent Classification）： 对应句子的整体语义。通常取 [CLS] 向量或对所有 Token 进行池化，接一个全连接层，输出为意图标签。
实体识别（Slot Filling）： 对应句子的局部信息。对每个 Token 的输出向量（不含 [CLS] 和 [SEP]）分别接全连接层，预测每个字符的标签（如 B-LOC, I-LOC 等）。
本质关系： 槽位（实体）往往受意图约束。例如，如果意图是“订机票”，那么实体更可能是“出发地/目的地”；通过多任务学习，模型能学习到这种语义上的相关性。
2. 分别用什么 Loss
代码中使用了 nn.CrossEntropyLoss()（交叉熵损失）分别计算两个任务：
分类 Loss ($L_{seq}$): 对 seq_output（整个句子的预测）和 seq_label_ids 计算交叉熵。
实体识别 Loss ($L_{token}$): 对 token_output（每个 Token 的预测）和 token_label_ids 计算交叉熵。
注意点： 代码中使用了 active_loss（通过 attention_mask 实现），这是为了忽略 Padding 部分的损失，只计算真实文本长度的 Loss。
3. Loss 求和的潜在坏处
代码中直接执行了 loss = seq_loss + token_loss。这种简单加和（Naïve Fusion）可能存在以下问题：
量级不一致： 分类是一个样本一个 Loss，而实体识别是一堆 Token 产生多个 Loss。如果 Token 数量很多，实体识别的 Loss 可能会淹没分类 Loss。
收敛速度不同： 某个任务可能很快就收敛（过拟合），而另一个任务还没学好。
梯度冲突： 两个任务对同一参数的梯度方向可能相反，导致模型在原地打转，谁也学不好。
4. 训练不平衡的处理方法
如果发现模型偏向于某一个任务，可以采用以下几种策略：
A. 加权求和 (Manual Weighting)
最直接的方法是给 Loss 分配权重：

根据实验结果调整 $\alpha$ 和 $\beta$。
B. 动态权重分配 (Uncertainty Weighting)
这是一种进阶方案，通过学习两个任务的**同方差不确定性（Homoscedastic Uncertainty）**来自动调整权重，公式大致为：

这里的 $\sigma$ 是模型自动学习的参数。
C. 训练策略优化
分阶段训练： 先冻结 BERT 预训练分类任务，再联合训练；或者交替训练（一个 Batch 练分类，下一个 Batch 练实体）。
学习率差异化： 给分类头和实体识别头设置不同的初始学习率。
D. 梯度裁剪或对齐 (GradNorm / PCGrad)
GradNorm： 动态调整 Loss 权重，使得各任务的梯度量级保持在同一水平。
PCGrad： 如果两个任务梯度方向冲突（余弦相似度为负），则将其中一个梯度投影到另一个梯度的法平面上，消除冲突。

